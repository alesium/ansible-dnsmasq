---
# defaults file for dnsmasq
# dnsmasq_conf_src: # define to overwrite the default
# dnsmasq_conf_templates:
#   - list of files to send into /etc/dnsmasq.conf.d
dnsmasq_conf_owner: root
dnsmasq_conf_group: root
